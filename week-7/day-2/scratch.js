// Imports
// const calculator = require('./calculator.js');
const { add, operations } = require('./calculator.js');

function func() {
  if (x) return 12

  if (y) return 12
  if (w) return 12
  if (q) return 12

  if (x || y || w || q) return 12
}

// console.log(calculator);
let first = 2;
let second = 2;

// console.log(calculator.add(first, second));
console.log(add(first, second));